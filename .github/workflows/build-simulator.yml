name: Build & Push Simulator

on:
  push:
    branches:
      - main
    paths:
      - "apps/simulator/**"
      - "packages/**"
      - ".github/workflows/build-simulator.yml"

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: apps/simulator/Dockerfile
          push: true
          tags: ghcr.io/${{ github.repository_owner }}/edge-iot-simulator:latest
