# RabbitMQ connection string (AMQP)
RABBITMQ_URL=amqp://guest:guest@localhost:5672

# HTTP / Socket.IO server port
PORT=4000

# Allowed CORS origin (the web dashboard)
CORS_ORIGIN=http://localhost:3003
